# Shared packages

Place reusable, language-specific libraries here when more than one application owns the dependency. Deployable Python, Scala, and TypeScript services belong under `apps/` instead.
