# Ignore MQTT broker data and logs
# Keep the directory structure but ignore the contents
data/*
!data/.gitkeep
log/*
!log/.gitkeep

# Ignore password and ACL files (if created later)
config/passwd
config/acl

# Ignore temporary files
*.tmp
*.bak
